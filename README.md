# Kubernetes sample manifest files
**Prerequisites**
- Should have kubernetes cluster to deploy manifests if not create one with [this](https://www.katacoda.com/courses/kubernetes)
- Should have some knowledge about YAML 


